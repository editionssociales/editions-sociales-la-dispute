/**
 * Ancienne nav mobile (bandeau hybride sticky + overlay plein écran).
 * Superseded by the responsive grid built directly into `SiteHeader`
 * (quadrillage brutaliste : maisons empilées + nav 2×2, sans JS ni overlay).
 * Conservé — vide — pour ne pas casser d'éventuels imports existants.
 */
export function SiteMenuMobile() {
  return null;
}
