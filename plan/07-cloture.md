Both contested points are verified: the embedded Next 16 docs confirm `permanent: true` → 308 / `false` → 307, with `statusCode` usable instead of `permanent` for exact 301/302; and the Vercel transfer doc (MàJ 2026-07-01) confirms integrations and Blob stores are NOT carried by a project transfer (env vars, domains, git link ARE), and that the initiator must be owner of the source team and member of the target team. I have everything needed to produce the final version.

# Phase 7 — Bascule finale et fermeture de la cohabitation

*Plan d'implémentation détaillé — version finale (révision post-relecture adversariale), 2026-07-09. S'appuie sur : IMPLEMENTATION-PROMPT.md (mandat, étape 7), COHABITATION.md (phase 4 « retrait progressif » — ce plan la termine et la dépasse), LEGACY-STACK.md (inventaire), DEVIS-MULTI-OPTIONS.md §5/§8/§9, et les recons R1–R4. Phase terminale : elle ne construit rien, elle **vérifie, éteint, transfère et documente**. Chaque extinction respecte le principe absolu n°1 : exports remis → vérifiés → extinction, jamais l'inverse. Deux corrections structurantes vs la première version : (1) le **transfert de propriété est exécuté à la mise en production de fin juillet** — pas à la clôture — parce que le devis §9 l'exige (« au plus tard à la mise en production », abonnements « payés par [la structure] dès la mise en production ») ; la Phase 7 en possède le protocole et en vérifie la complétude, elle ne le date pas ; (2) les **fenêtres de rollback sont réelles** : à l'extinction, on rend le WordPress injoignable (DNS + détachement) mais fichiers ET base restent intacts 7 jours — rien n'est supprimé le jour J.*

---

## Objectif et livrable

**Objectif.** Fermer le chantier : éteindre le dernier WordPress du périmètre (Boutique), solder les deux WordPress catalogue s'il reste un reliquat de la phase catalogue, rendre les redirections définitives (301, sur validation écrite du client — devis « après votre validation »), purger le code et l'infra de tout ce qui référençait WordPress, **vérifier** que la propriété complète est transférée au client (le transfert lui-même a été exécuté à la mise en production, fin juillet — devis §9), remettre le dossier de réversibilité, et prononcer la clôture sur des critères objectifs.

**Livrables concrets :**
1. **État final OVH atteint et documenté** — RESTE : les 6 domaines, Email Pro (`emailpro-lr23975-2`), le slot Pro `editionssociales.fr` ne portant plus que GEME/BioMarx (`gememarxengels.org`), les services mail des zones (`ladispute.fr` redirect, `la-dispute.fr` MXPLAN 1000 — vérifié, intouché) ; PARTI : slot Performance `ladispi` résilié (fait en phase prod, vérifié ici), front-ends WP `www`/`LaDispute`/`Boutique` éteints (fichiers archivés puis retirés **après fenêtre tampon**), bases `editionskes`/`editionsk712`/`editionsk884` dumpées puis supprimées, base Joomla `1413613-1` dumpée puis supprimée.
2. **Archive complète remise et confirmée** : dumps SQL des 3 WP + CSV commandes/clients/abonnés + uploads + manifeste sha256, avec confirmation écrite de réception par le client.
3. **Redirections 301 définitives** sur toutes les familles d'URL legacy (dont `/produit/<slug>/` → fiches livre via la table de matching de la migration commerce, et le host `www.boutique.*` couvert au même titre que l'apex).
4. **Repo nettoyé** : adaptateur http WordPress retiré (décision ci-dessous : **retirer**, pas garder), variables `WP_ES_URL`/`WP_LD_URL`/`WC_STORE_URL`/`WP_REVALIDATE` supprimées (code + Vercel), `remotePatterns` OVH retirés de `next.config.ts`, `mysql2` sorti des dépendances runtime, docs (`README.md`, `COHABITATION.md`, `LEGACY-STACK.md`) mises à l'état final.
5. **Propriété transférée — vérifiée complète** : repo GitHub sous le compte client `editionssociales`, projet Vercel sous la team client `ldes` **avec les ressources Marketplace (Neon, Blob) transférées séparément** (la doc Vercel « Transferring a project », MàJ 2026-07-01, est explicite : les intégrations et le store Blob ne suivent PAS le transfert de projet), comptes stack (Stripe, Brevo, Sentry, Better Stack) au nom de la structure, Youri invité partout. Exécuté fin juillet (mise en production) ; la clôture vérifie et solde les reliquats.
6. **Dossier de réversibilité + runbook d'exploitation** remis (devis §9 : « aucun service ne vous enferme »).
7. **PV de recette de clôture** signé = définition objective de « fini » + modalités de maintenance actées (corrective gratuite, pas d'abonnement, devis §9). Le PV consigne aussi l'unique geste post-signature (drop des bases à J+7) comme **engagement daté avec preuve à fournir** — aucune autre action destructrice après signature.

---

## Préconditions et provisioning (comptes, clés, accès — qui fait quoi, client vs Youri)

**Gates d'entrée (rien ne démarre sans) :**

| # | Précondition | Produite par | Vérification d'entrée |
|---|---|---|---|
| G1 | Catalogue lit Postgres en prod (`src/lib/catalogue.ts` n'instancie plus `httpCatalogueSource()` pour `listBooks/getBook`) ; les 2 WP catalogue sont éteints **ou** en fin de fenêtre de rollback | Phase catalogue | grep du repo + état OVH `attachedDomain` |
| G2 | Commerce natif en prod ≥ durée de la fenêtre de recouvrement (défaut 14 j) : commandes réelles encaissées Stripe, grille de port recopiée validée en recette. **Le solde de la phase commerce est dû à cette recette** (devis : « le solde de chaque phase à sa recette ») — la clôture d'octobre n'ajoute aucun paiement | Phase commerce | Dashboard Stripe + PV recette commerce |
| G3 | Archive boutique produite et **remise** (ligne devis 0,25 j « Archives : 5 753 commandes + clients ») — Phase 7 la re-vérifie, ne la découvre pas | Phase commerce | Manifeste sha256 + accusé client |
| G4 | Newsletter migrée Brevo (les tables `mod973_newsletter*` vivent dans `editionsk884` — le drop de la base boutique détruit la source) | Phase newsletter | Compte Brevo : ≥ 2 848 contacts importés |
| G5 | DNS boutique déjà flippé vers Vercel avec redirections **302** au cutover commerce — **apex ET `www.boutique.editionssociales.fr`** (la zone a des A/AAAA sur `www.boutique`, R4 : les deux hostnames doivent être attachés au projet Vercel et couverts par la table de redirects, sinon les vieux liens en `www.boutique` tombent sur une erreur OVH après détachement) ; WP boutique gelé mais vivant sur hostname technique (`cms-boutique.*`) | Phase commerce / prod | `dig boutique.*` + `dig www.boutique.*` + curl 302 sur les deux hosts |
| G6 | **Slot `ladispi` résilié OU décision client documentée de le conserver, avec l'inventaire des boîtes mail au dossier.** La phase 2 (E8) a découvert le couplage slot/MXPLAN — 4 boîtes mail actives sur `la-dispute.fr` (offre « MXPLAN 1000 hosting », probablement incluse dans l'hébergement) — qui rend l'option « garder le slot » légitime (décision client Q5 de la phase 2). Si la voie résiliation a été prise : zone `la-dispute.fr` repointée **avant** (A `@`/`www` quittent 46.105.204.28) et boîtes préservées (gate E8.1 phase 2 : confirmation OVH écrite ou migration préalable — la résiliation ne vise que `/hosting/web/ladispi…`, jamais `/email/*`) | Phase prod (phase 2, E8) | Si résilié : API OVH `/hosting/web` ne liste plus `ladispi…` + `dig la-dispute.fr` + test des 4 boîtes ; si conservé : décision écrite (Q5 phase 2) + inventaire des boîtes au dossier |
| G7 | **Dépendance Legacy REST API tracée avant coupure** (engagement devis §3 : « sa dépendance est tracée avant extinction ») : livrable phase commerce = identification par la compta de l'outil consommant l'export de commandes (prérequis client du devis §10) **+ logs d'accès prod sur `/wc-api/*` et l'API legacy pendant la fenêtre de recouvrement = zéro consommateur restant** ; et export compta natif validé par la compta sur une vraie période comptable. R2 §3 a déjà établi 0 clé API / 0 webhook et Paybox comme seul utilisateur de `wc-api` (mécanisme cœur WC, pas le plugin legacy) — cette gate transforme ce constat sur dump en preuve datée côté prod | Phase commerce | Preuve datée (logs + attestation compta) archivée au dossier de clôture |
| G8 | **Transfert de propriété exécuté à la mise en production (phase 2, E9 — en fin de fenêtre de bascule unique, 24–28/07 proposée, cf. `plan/02-mise-en-production.md`)** selon le protocole de l'étape 9, protocole de référence du projet : repo GitHub sous `editionssociales`, projet Vercel sous `ldes` (Pro payé par le client), intégration Neon et store Blob transférés, Stripe/Brevo/Sentry/Better Stack au nom de la structure. Si le client a explicitement accepté **par écrit** un report (dérogation au §9), la dérogation datée est au dossier et le transfert s'exécute à l'étape 9 de la semaine de clôture | Phase 2 (E9) — protocole : étape 9 de cette phase | Preuves de propriété (étape 9) ou dérogation écrite |

Si G1/G5/G6 n'ont pas été soldés par leurs phases, la Phase 7 les rattrape (étapes 2 et 8) — c'est prévu dans l'effort. Si G7 est rouge, **aucune extinction boutique** tant que la preuve n'existe pas — les WP gelés ne coûtent rien à attendre.

**Accès — Youri :**
- API OVH compte ES : `ovh-use es` + helper `~/.config/claude/ovh_api.py` (LEGACY-STACK §1.1). Sert pour : dumps DB (`POST /hosting/web/editionssociales.fr/database/{db}/dump`), détachement de domaines (`DELETE …/attachedDomain/{domain}`), suppression de bases (`DELETE …/database/{db}`), zones DNS (`/domain/zone/*`).
- SSH/SFTP du slot Pro (récupération `wp-content` + suppression des dossiers) — vérifier/réinitialiser l'utilisateur FTP via l'API OVH **avant** la semaine de clôture.
- Tokens shell `GH_TOKEN` (yourimerad) et `VERCEL_TOKEN` (team solidz) — ⚠️ R4 : **pas** ceux de `site/.env`, qui pointent les coquilles client. Après le transfert de fin juillet, ce sont les accès « invité » sur les comptes client qui font foi.
- MariaDB locale port 3307 pour les tests de restauration (README du repo, `.localdb`).

**Accès — Client (à provisionner AVANT la mise en production de fin juillet — relance à J-7, PAS au 28/09) :**
- Compte GitHub `editionssociales` : identifiants en main, capable d'**accepter un transfert de repo** (le `GITHUB_PAT` de `site/.env` prouve que le compte existe — R4).
- Team Vercel `ldes` : **moyen de paiement valide + souscription Vercel Pro (~20 €/mois, devis §8) dès fin juillet** — c'est une exigence contractuelle (§9 : abonnements « payés par [la structure] dès la mise en production ») ET une précondition documentée du transfert (« If the target Vercel team does not have a valid payment method, you must add one before transferring »). **Inviter Youri membre de `ldes`** : la doc Vercel exige que l'initiateur du transfert soit owner de la team source ET membre de la team cible ; alternative : le client initie lui-même via le mécanisme de transfer request (`POST /projects/{id}/transfer-request` → code accepté côté `ldes`).
- Confirmation des propriétaires effectifs de Stripe (compte live), Brevo, Sentry (org UE), Better Stack — créés au nom de la structure par les phases amont ; on vérifie et on corrige si un compte a été ouvert au nom de Youri.
- Un espace de réception d'archive côté client (Drive/disque de la structure) : l'archive ne doit pas vivre uniquement sur l'infra transférée.

**Interdits absolus rappelés :** aucun enregistrement MX/SPF/DKIM/DMARC modifié (état hybride fragile documenté R4 §3.3) ; jamais de résiliation du slot Pro (GEME y vit) ; aucune suppression avant export remis **et** vérifié ; aucune destruction entre la signature du PV et la preuve du drop planifié autre que ce drop lui-même.

---

## Étapes (ordonnées, numérotées)

### Étape 1 — Audit d'entrée et gel du plan (0,25 j)
**Quoi.** Dérouler les gates G1–G8 et produire la photo « avant clôture » : sortie brute de `GET /hosting/web/editionssociales.fr/attachedDomain`, `GET …/database`, export des zones DNS des 6 domaines (`GET /domain/zone/{zone}/export`), liste des env vars Vercel, `git log -1` de `main`. Cette photo est l'annexe « état initial » du PV de clôture et le diff de référence pour vérifier que seuls les A/CNAME web ont bougé. Pour G7 : classer la preuve datée (logs `/wc-api/*` sur la fenêtre + attestation compta sur l'export natif) au dossier.
**Fichiers/ressources.** API OVH, API Vercel, repo. Photo archivée dans le dossier de clôture (hors repo public si elle contient des hostnames techniques).
**Vérifier.** Chaque gate a une preuve datée ; tout gate rouge → l'étape de rattrapage correspondante est insérée avant de continuer ; G7 rouge = arrêt de la chaîne d'extinction boutique.

### Étape 2 — Purge de la file boutique et re-vérification de l'archive (0,25 j)
**Quoi.** (a) Avec le client : solder les commandes `wc-processing`/`wc-on-hold` restantes dans wp-admin boutique (expédier ou annuler — R2 en comptait 107 au dump, le chiffre réel est celui du gel). (b) Depuis le gel (G5), plus aucune écriture n'est possible publiquement ; prendre le **dump SQL final** de `editionsk884` (API OVH dump, format `mysql8.0` ou via phpMyAdmin) — c'est lui qui fait foi, pas celui de la phase commerce s'il reste des commandes soldées entre-temps. (c) Reconstituer l'archive complète (contenu détaillé en section Données) et le manifeste sha256.
**Vérifier.** Restauration du dump dans MariaDB 3307 (base jetable) : comptages contre la prod gelée — `shop_order` (≥ 5 753 + delta depuis le 2026-07-01), `mod973_users` (≥ 1 220), `wc_customer_lookup` (≥ 1 329), produits publiés (223 ± mouvements), `mod973_newsletter` status `C` (2 848 ± désinscriptions), et `wc-pending` = 0. ⚠️ HPOS désactivé : compter dans `mod973_posts`, **pas** dans `mod973_wc_orders` (vides — R2 §2.3). Échantillon : 10 commandes tirées au sort comparées écran wp-admin ↔ CSV ↔ dump.

### Étape 3 — Remise de l'archive et confirmation écrite (0,25 j, dont attente client)
**Quoi.** Remettre l'archive (lien de téléchargement + manifeste) sur l'espace du client ; demander une **confirmation écrite** explicite : « archive reçue, ouverte, CSV commandes lisible, nombre de lignes conforme ». Modèle d'email fourni avec les 3 vérifications à faire (ouvrir le CSV dans un tableur, compter, vérifier une commande connue).
**Vérifier.** L'email de confirmation existe. **Aucune extinction avant.** C'est le verrou contractuel du devis §9 (« une extinction ne détruit jamais de données sans qu'un export complet vous ait été remis avant »).

### Étape 4 — Redirections définitives : 302 → 301, sur validation écrite (0,25 j)
**Quoi.** Deux préalables, puis le flip :
- **Validation client écrite d'abord** (devis : redirections « définitives après votre validation ») : un email suffit — « la table 302 tourne depuis N semaines, revue des 404 dans les logs Vercel : zéro anomalie, OK pour rendre définitif ? ». Ne pas flipper sans cette réponse.
- **Codes de statut exacts** : dans `next.config.ts`, `permanent: true` produit un **308** (et `false` un 307), pas un 301/302 — confirmé dans les docs Next 16 embarquées (`node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/redirects.md`). Le devis parle de « 302 puis 301 » et la recette est contractuelle : utiliser la propriété **`statusCode: 301`** (documentée : « you can use the `statusCode` property instead of the `permanent` property, but not both ») pour que les scripts curl et le critère 1 de la recette voient littéralement des 301. (Les 302 des phases amont auraient dû être posés en `statusCode: 302` — si elles ont utilisé `permanent: false`/307, l'aligner ici sans autre impact.)
Familles couvertes, avec `has: [{ type: "host", value: … }]` **pour l'apex `boutique.editionssociales.fr` ET pour `www.boutique.editionssociales.fr`** (deux entrées ou pattern équivalent — le `has` host ne matche pas les sous-domaines implicitement) :
- `/produit/:slug` → fiche livre via la **table de matching produite par la migration commerce** (~204 correspondances slug→(edition,slug), ~9 liens historiquement cassés rattrapés à la main, 20 produits orphelins → leur page native ou `/catalogue` — interface Phase 6) ; générée en fichier data (`src/lib/redirects-produits.json` ou équivalent), pas saisie à la main.
- `/panier`, `/commander`, `/mon-compte` → `/panier` (ou équivalents natifs) ; `/categorie-produit/:cat` → `/catalogue/...` via table catégories (product_cat « La Dispute »→`/catalogue/la-dispute`, « Editions sociales »→`/catalogue/editions-sociales`, collections → filtres) ; `/` (hosts boutique) → `/catalogue`.
- Domaines catalogue : les redirections posées en phase prod (`editionssociales.fr`/`ladispute.fr` anciennes URLs WP) passent aussi en `statusCode: 301`. `editions-sociales.fr`/`.com` : **aucune redirection OVH n'existe** — vérifié par la phase 2 (API : `GET /domain/zone/…/redirection` → `[]` ; HTTP live : page parking « Site en construction »). La Phase 7 hérite de l'état réel : soit l'option **E8.4bis** de la phase 2 a été prise (domaines attachés au projet Vercel en « Redirect to Primary ») et ils redirigent déjà, soit ils sont restés en parking — dans les deux cas, **rien à vérifier côté redirection OVH** ; ne rien toucher d'autre.
**Vérifier.** Script curl sur un échantillon écrit : les 20 produits les plus commandés (extraits de `order_itemmeta` du dump), 5 URLs de catégories, panier/checkout/mon-compte, **le tout doublé sur `www.boutique.*`** → chaque URL répond `301` puis `200` sur la cible. `pnpm build` vert (les redirects sont compilés au build).

### Étape 5 — Extinction du WordPress Boutique, phase A : mise hors ligne réversible (0,25 j)
**Quoi.** Dans l'ordre, après l'étape 3 uniquement. **Rien n'est supprimé à cette étape — ni fichiers ni base** : l'extinction se fait en deux temps pour que le rollback < 1 h promis existe réellement pendant toute la fenêtre tampon (7 jours). Le jour J :
1. Archive tar du dossier `Boutique/` via SFTP tant que tout est en place (les miroirs locaux sont périmés de quelques semaines — R3) : au minimum `wp-content/uploads` (441 Mo) et `wp-config.php` **expurgé des secrets**, versés au dossier de réversibilité.
2. Supprimer l'enregistrement DNS technique `cms-boutique.*` (le WP devient injoignable publiquement).
3. `DELETE /hosting/web/editionssociales.fr/attachedDomain/boutique.editionssociales.fr` + `www.boutique.*` + le hostname technique — le slot Pro ne route plus rien vers le dossier `Boutique`.
4. **Nettoyage Stripe** : dans le dashboard du compte Stripe du client, désactiver/supprimer les endpoints webhook de l'ancienne gateway WooCommerce (`…boutique.editionssociales.fr/?wc-api=…` — y compris les webhooks *test* créés lors de la tentative de config de juin 2026, R2 §2.4) : sinon échecs de livraison répétés, mails d'alerte Stripe et confusion en debug des webhooks du site neuf. Consigner dans le RUNBOOK : « webhooks restants = uniquement ceux du site neuf ».
5. Les fichiers du dossier `Boutique/` et la base `editionsk884` **restent intacts sur le slot** jusqu'à l'étape 8 (fenêtre tampon d'une semaine). Rollback pendant la fenêtre : re-créer le record `cms-boutique` + `POST …/attachedDomain` → boutique WP de retour en < 1 h, données et fichiers d'origine.
**Vérifier.** Tester **ce qui change réellement** (les curls sur le domaine public sont déjà verts depuis le flip DNS de la phase commerce — ils ne prouvent rien ici) : `dig cms-boutique.editionssociales.fr` → NXDOMAIN (après TTL) ; `GET …/attachedDomain` ne liste plus `boutique.*` ni `www.boutique.*` ; **`https://gememarxengels.org` répond 200 inchangé** ; envoi/réception d'un email `toutes@editionssociales.fr` OK ; diff de zone : seuls A/CNAME modifiés. Non-régression côté public : `/produit/<slug-connu>/` → 301 → 200 sur les deux hosts.

### Étape 6 — Rattrapage catalogue si nécessaire (0–0,25 j, conditionnel)
**Quoi.** Si la phase catalogue a laissé `www`/`LaDispute` vivants en fin de fenêtre : **même protocole en deux temps qu'étape 5** — (jour J) dump final `editionskes`/`editionsk712`, archive `wp-content` (les uploads ES+LD ≈ 1 Go ; seuls originaux+PDF ont été migrés en phase 3 (R3 §7), mais l'archive complète part quand même au client, c'est sa donnée), suppression des records `cms-es`/`cms-ld`, détachement des domaines résiduels ; (étape 8, J+7) suppression des dossiers `www/` et `LaDispute/` et drop des bases.
**Vérifier.** `dig cms-es.*`/`cms-ld.*` → NXDOMAIN ; `GET …/attachedDomain` ne liste plus les entrées catalogue résiduelles ; le catalogue du site neuf affiche toujours 295+ livres (preuve que plus rien ne dépend de WP).

### Étape 7 — Nettoyage du code : sortir WordPress du repo (0,25 j)
**Quoi.** Décision (mandat : « garder ou retirer ? ») : **retirer l'adaptateur http, garder le port**. Rationale : les défauts codés en dur de `catalogue-http.ts:16-17` et `boutique.ts:13` pointent les domaines publics ; après cutover ces domaines servent le nouveau site → un adaptateur http réinstancié fetcherait du HTML sur soi-même et la dégradation résiliente (R1 §1) **masquerait** l'erreur en catalogue vide. Code mort + piège silencieux = on retire. Git garde l'histoire (tag `wordpress-era-end` posé sur le dernier commit avec l'adaptateur).
- Supprimer : `src/lib/catalogue-http.ts`, la partie fetch Store API de `src/lib/boutique.ts` (ou le fichier si `listProducts` vient de l'adaptateur Postgres), toute lecture de `WP_ES_URL`/`WP_LD_URL`/`WC_STORE_URL`/`WP_REVALIDATE`.
- Garder : `catalogue-source.ts` (port + shapes + in-memory), `catalogue-core.ts` (la normalisation reste utile si l'adaptateur DB a mimé les shapes bruts — option (a) de R1 §1), `types.ts`, les 55 tests (aucun n'importe l'adaptateur http — graphe R1 §7).
- `next.config.ts` : retirer les `remotePatterns` OVH `/wp-content/**` — **précondition : zéro URL OVH résiduelle en base** (voir Données).
- `package.json` : retirer `mysql2` du runtime (le déplacer en devDependency si des scripts de migration sous `scripts/` s'en servent encore, sinon supprimer).
- Env Vercel : supprimer `WP_ES_URL`/`WP_LD_URL`/`WC_STORE_URL`/`WP_REVALIDATE` (cibles production + development) ; nettoyer `.env.example`/`.env.local`.
- `wp-headless/` : **garder dans le repo**, bandeau « historique — contrat des archives WordPress, infra éteinte le JJ/MM » (il documente la provenance des données migrées ; coût nul).
- Docs : `README.md` réécrit (il est déjà périmé — R1 surprise 1), `COHABITATION.md` clos (phase 4 ✔ + état final), `LEGACY-STACK.md` bandeau « historique, infra éteinte le … » sans le réécrire.
**Vérifier.** `grep -rE "WP_ES_URL|WP_LD_URL|WC_STORE_URL|wp-json|catalogue-http" src/` → 0 hit ; `pnpm typecheck && pnpm lint && pnpm test && pnpm build` verts ; déploiement prod OK ; navigation complète (accueil, catalogue, fiche, panier, souscription) sur la prod post-déploiement.

### Étape 8 — État final OVH : suppression des fichiers, des bases et des scories (0,25 j, à J+7)
**Quoi.** Une semaine après l'étape 5 sans incident — c'est **l'unique action destructrice post-PV**, consignée comme engagement daté dans le PV (voir Recette, critère 2) :
1. Re-dump de contrôle, puis suppression des fichiers des dossiers `Boutique/` (et `www/`/`LaDispute/` si étape 6) — les archives locale + client existent depuis les étapes 5/6.
2. `DELETE /hosting/web/editionssociales.fr/database/{editionsk884,editionskes,editionsk712}` (les 3 dumps finaux sont déjà dans l'archive remise). Motivation au-delà de la propreté : 1 329 clients + 1 220 users dans une base plus maintenue = passif RGPD inutile.
3. Base Joomla `1413613-1` : dump → archive → suppression (question LEGACY-STACK §11 résolue par R2 : ancêtre abandonné).
4. `dev.editionssociales.fr` : sort tranché par le client (question ouverte Q4) — défaut : archiver le dossier, détacher le domaine, supprimer les records DNS `dev`/`www.dev`.
5. Supprimer les enregistrements DNS morts (`cms-*` restants). **Ne pas toucher** : MX/SPF/DKIM/DMARC (y compris DKIM Brevo, désormais utile), records GEME, zones `editions-sociales.fr`/`.com` (redirections), services mail `ladispute.fr`/`la-dispute.fr`.
6. Monitoring : retirer les moniteurs Better Stack des 3 WP (ils hurleraient à vide) ; conserver ceux du site neuf + heartbeat backup.
7. `editionsk896` (GEME) et le slot Pro : **intouchés** ; noter dans le PV que le slot Pro expire le **2026-12-01** et doit être renouvelé par le client (GEME en dépend).
8. **Envoyer au client la preuve écrite** : capture de `GET /hosting/web/editionssociales.fr/database` (une seule base restante : `editionsk896`) + listing SFTP — c'est le second temps du critère 2 de la recette.
**Vérifier.** `GET …/database` ne liste plus que `editionsk896` ; les dossiers WP ont disparu du slot (listing SFTP) ; GEME répond 200 ; email test OK ; facture OVH suivante = domaines + Email Pro + slot Pro seulement (~15,6 €/mois, tableau stack).

### Étape 9 — Transfert de propriété : protocole de référence du projet (exécuté par la phase 2 — E9, en fin de fenêtre de bascule unique, 24–28/07 proposée, cf. `plan/02-mise-en-production.md` ; vérifié et soldé ici)
**Calage contractuel.** Le devis §9 est sans ambiguïté : transfert « au plus tard à la mise en production », abonnements « souscrits au nom de votre structure et payés par elle dès la mise en production ». La mise en production (dons + catalogue **et commerce**, désormais dans la même fenêtre de bascule unique — cf. `plan/02-mise-en-production.md`, 24–28/07 proposée) a lieu **fin juillet** → **le transfert s'exécute fin juillet**, pas à la clôture. Youri reste ensuite membre/invité sur les comptes client pour continuer le chantier — c'est exactement le modèle « j'interviens comme invité sur vos comptes, jamais l'inverse ». Seule une **dérogation écrite du client** (proposée, pas suggérée comme défaut) peut reporter l'exécution à la semaine de clôture ; la carte + Vercel Pro sur `ldes` sont provisionnés fin juillet **dans tous les cas** (le paiement par la structure dès la mise en production n'est pas dérogeable sans réécrire le devis). **Ce protocole est LE protocole de référence du projet** : la phase 2 l'exécute (E9, en fin de fenêtre de bascule unique, après le dry-run 9-bis calé au 17–20/07 — date exacte non figée tant que la fenêtre 24–28/07 n'est pas actée par le client), les plans des phases 2 et 6 y **renvoient** désormais au lieu de décrire le leur, et la Phase 7 le vérifie et le solde en octobre (rotation des tokens, preuve de propriété).

**Protocole (ordre : repo d'abord, pour re-lier Vercel une seule fois) :**
1. **GitHub** : merger/fermer les branches mortes (`worktree-*`, PR #1), transfert `yourimerad/editions-sociales-la-dispute` → compte `editionssociales` (transfert natif GitHub, historique conservé, le client accepte depuis son compte) ; Youri ré-invité collaborateur (write). ⚠️ **Pas de protection de branche possible sur la cible par défaut** : le repo est privé et la branch protection/rulesets exigent GitHub Pro/Team/Enterprise sur les repos privés — sur le compte utilisateur `editionssociales` en plan Free, elle n'est pas disponible. Options dans Q3 ; défaut : absence acceptée et documentée dans le RUNBOOK (un seul dev, CI sur PR déjà en place).
2. **Vercel — chemin (b) par défaut, le chemin (a) « le transfert emporte tout » est exclu par la doc** (« Transferring a project », MàJ 2026-07-01) : le transfert de projet transporte déploiements, env vars, domaines/alias, lien Git, cron — mais « Integrations … must be added again after the transfer » et Vercel Blob a « a separate transfer mechanism ». Concrètement : (i) installer la GitHub App Vercel sur le compte `editionssociales` ; (ii) transférer le projet `solidz` → `ldes` (initiateur = owner de `solidz` ET membre de `ldes` — Youri invité au préalable, ou transfer request initiée côté client) ; (iii) **transférer séparément l'intégration Neon** (mécanisme « Transferring an Integration » côté Vercel, ou transfert de la ressource côté Neon) et **le store Blob** (mécanisme de store transfer) vers `ldes` ; (iv) filet si un des deux transferts de ressource bloque : re-création sous `ldes` + `pg_dump | pg_restore` (~1 h, c'est l'intérêt du Postgres standard) + copie Blob par script (~1 h, fichiers plats HTTP) ; (v) fallback ultime : transfert de la **team** `solidz` elle-même (client owner, Youri membre) — moins propre, mais garanti.
3. Re-lier le projet Vercel au repo transféré (production branch `main`), re-déployer, vérifier la prod.
4. **Comptes stack** : vérifier propriétaire = structure pour Stripe (live), Brevo, Sentry (org UE), Better Stack ; corriger sinon (changement d'owner) ; Youri = invité partout.
5. **Secrets** : rotation — les tokens `yourimerad`/`solidz` du shell perdent leur objet à la clôture ; les tokens client de `site/.env` (surprise R4 : ils pointaient déjà les coquilles `editionssociales`/`ldes`) deviennent les bons ; régénérer proprement les deux et re-provisionner `STRIPE_SECRET_KEY` live côté env Vercel du projet transféré si nécessaire. Ne jamais afficher une valeur.

**Vérifier — preuve de PROPRIÉTÉ, pas de fonctionnement.** Un test fonctionnel (jauge de dons, commande test) est un **faux positif** : les env vars étant copiées par le transfert, le site continue de pointer la même base Neon même si elle est restée possédée et facturée sous `solidz`. Les critères sont : (i) le **client** se connecte lui-même et voit le repo sous son compte, le projet sous sa team **avec facturation sur sa carte** ; (ii) **les ressources Neon et Blob apparaissent dans le dashboard `ldes` et n'apparaissent plus sous `solidz`** ; (iii) un `git push main` de test déclenche build + prod ; (iv) le test fonctionnel (jauge + commande/don de test) vient **en plus**, comme non-régression.

**À la semaine de clôture (0,25 j)** : re-dérouler ces preuves (G8), solder les reliquats (rotation finale des tokens Youri, ménage de la team `solidz`), et re-vérifier que rien n'est revenu sous `solidz` entre-temps.

**Étape 9-bis (dry-run calé au 17–20/07, avant l'exécution d'E9 en fin de fenêtre de bascule unique — 24–28/07 proposée, cf. `plan/02-mise-en-production.md` — plus octobre) :** dry-run documentaire et pratique — lire la doc Vercel du moment (« Transferring a project », « Transferring an Integration », store transfer) + tester sur un projet jetable avec une ressource Marketplace factice. Résultat consigné, procédure exacte (b)(iii) vs (b)(iv) choisie **avant** le jour J.

### Étape 10 — Dossier de réversibilité + runbook (0,5 j)
**Quoi.** Deux documents remis (PDF/Markdown) + l'archive :
- **REVERSIBILITE** : inventaire de l'archive (contenu, checksums), procédure de restauration WordPress pas-à-pas (n'importe quel hébergeur PHP : fichiers + dump + `wp-config`), procédure de sortie de la stack neuve (restaurer un `pg_dump` nocturne ailleurs, copier le Blob, déployer le Next hors Vercel — standards ouverts, devis §9), emplacement et rétention des sauvegardes nocturnes (GitHub Actions → Blob, 30 j, heartbeat), inventaire DNS final annoté (« ne jamais toucher MX »), liste des comptes avec propriétaire et rôle de chacun.
- **RUNBOOK exploitation** : gestes quotidiens (fiches via back-office, produits, exports compta), quoi faire si le site tombe (Better Stack → Sentry → Vercel), renouvellements à ne pas rater (slot Pro OVH 2026-12-01, domaines, Vercel Pro), hygiène Stripe (webhooks restants = site neuf uniquement ; contrat Paybox devenu sans objet — résiliation par le client, Q9), absence de protection de branche GitHub si Q3 = défaut (et comment l'activer si passage à Pro/org), contact maintenance et modalités (§9 : corrective gratuite, nouveau besoin chiffré, aucun abonnement).
**Vérifier.** Une restauration est **démontrée**, pas seulement documentée : restauration d'un `pg_dump` nocturne dans une base Neon jetable + spot-check de 5 livres ; restauration du dump WP boutique en local (déjà faite étape 2, référencée).

### Étape 11 — Recette de clôture et PV (0,25 j, avec le client)
**Quoi.** Dérouler ensemble la checklist de recette (section suivante), signer le **PV de clôture** qui contient : critères cochés avec preuves, état final OVH **avec l'engagement post-signature explicite** (drop des bases à J+7, preuve écrite à fournir — critère 2), engagements de maintenance (§9), rappels de renouvellement, et le **rappel des références de paiement** : le solde de chaque phase a été dû à sa propre recette (devis §9 — le solde commerce désormais dû à la recette de la fenêtre de bascule unique, plus la clôture d'octobre) ; **la clôture ne déclenche aucun paiement nouveau**, sauf reliquat explicitement convenu. Le chantier est déclaré clos à la signature, sous réserve du seul engagement post-signature consigné.

---

## Données et migration

Pas de migration nouvelle dans cette phase — uniquement **archives et vérifications**. La phase est le verrou qualité des migrations des phases amont.

**Contenu exact de l'archive remise (étapes 2–3, complétée étape 6/8) :**

| Pièce | Source exacte | Vérification |
|---|---|---|
| Dump SQL final `editionsk884` (Boutique, ~95 Mo) | API OVH `POST /hosting/web/editionssociales.fr/database/editionsk884.mysql.db/dump` (ou phpMyAdmin), **après gel et purge** | Restauration MariaDB 3307 ; comptages `shop_order`/`users`/`customer_lookup`/produits/`newsletter` vs prod gelée (base : 5 753 / 1 220 / 1 329 / 223 / 2 848 au 2026-07-01 + delta) ; `wc-pending` = 0 ; **compter dans `mod973_posts`, HPOS vide** |
| CSV commandes (5 753+) | Généré depuis le dump : `mod973_posts` (shop_order) + `mod973_postmeta` (`_billing_*`, `_order_total`, `_payment_method`, `_transaction_id`, dates) + `mod973_woocommerce_order_items`/`order_itemmeta` (R2 §5) — même périmètre de colonnes que l'export Advanced Order Export utilisé par la compta (`woe_order_exported` sur 4 888 commandes) | Nb lignes = nb `shop_order` ; 10 commandes échantillonnées ↔ wp-admin ; ouverture tableur par le client |
| CSV clients (1 329) | `mod973_wc_customer_lookup` + `mod973_users`/`usermeta` | Nb lignes ; 5 clients échantillonnés |
| CSV abonnés newsletter (2 848 `C` + 7 `U`) | `mod973_newsletter` (colonnes `email,name,surname,status,created,ip,…`) — redondant avec l'import Brevo de la phase newsletter, mais l'archive doit être autoportante | Diff avec le compte Brevo |
| Dumps SQL finaux `editionskes`, `editionsk712` | API OVH dump (étape 6 ou déjà remis en phase catalogue — re-vérifiés) | Restauration + comptage `catalogue` publiés (117/178 au 2026-07-09) |
| Dump `1413613-1` (Joomla historique) | API OVH dump | Archivage simple, pas de comptage |
| `wp-content/uploads` des 3 installs (~1,43 Go : 427+562+441 Mo) + thèmes `cenote_child` (le CPT y est déclaré — c'est le schéma des dumps) + mu-plugin | SFTP prod (les miroirs locaux sont périmés — R3 §5), pris **avant** tout détachement (étapes 5.1/6) | Taille et nb fichiers vs `du`/`find` sur le slot ; sha256 |
| Manifeste `SHA256SUMS` + note de contenu | Généré localement | `shasum -c` par le client ou avec lui |

**Vérification transverse avant le retrait des `remotePatterns` (étape 7) :** requête SQL sur la base Postgres de prod — zéro occurrence de `editionssociales.fr/wp-content`, `ladispute.fr/wp-content`, `boutique.editionssociales.fr` dans les champs HTML (`presentation`, `furtherReading`) et les URLs de médias. Toute occurrence = hotlink qui cassera à l'extinction → corrigée (réécriture vers Blob) **avant** l'étape 5. (R3 §6 : ~190 réfs de médias embarquées dans le HTML éditorial — la phase catalogue devait les réécrire ; on le prouve ici.)

**Rollback (fenêtres réelles, alignées sur les étapes 5/6/8) :**
- **Pendant la fenêtre tampon (étape 5 → étape 8, 7 jours)** : fichiers ET base intacts sur le slot → re-créer le record `cms-*` + `POST …/attachedDomain` = WordPress de retour en **< 1 h, réellement** (rien à re-téléverser). Les commandes passées sur le système neuf pendant la fenêtre restent dans le système neuf (pas de double saisie WP) — procédure notée au PV si rollback.
- **Après l'étape 8 (suppression)** : restauration depuis l'archive sur tout hébergeur PHP (procédure du dossier REVERSIBILITE) — des heures, pas des minutes, et c'est assumé : c'est pour ça que la fenêtre tampon existe et que rien n'est supprimé avant elle. Rien n'est irréversible, c'est le point du dossier.

---

## Recette et critères d'acceptation (point de vue client, testables)

Le chantier est « fini » quand **tous** les critères ci-dessous sont cochés en séance avec le client (critère 2 : premier temps en séance, second temps sur preuve écrite) :

1. **Anciennes adresses** : sur une liste écrite de 30+ URLs legacy (20 produits les plus vendus, 5 catégories, panier, checkout, mon-compte, accueil boutique, 2 fiches catalogue WP — échantillon doublé sur `www.boutique.*`), chacune répond **301** (statusCode explicite, conforme au « 302 puis 301 » du devis) → page équivalente 200 du nouveau site — démontré au navigateur.
2. **Plus de WordPress dans le périmètre — en deux temps consignés au PV** : *(en séance)* `cms-boutique.*`/`cms-es`/`cms-ld` ne résolvent plus (NXDOMAIN), le manager OVH (le client regarde lui-même) ne montre plus aucun attachedDomain boutique/catalogue, les dumps finaux sont remis et confirmés, et **le drop des fichiers + bases est PLANIFIÉ à J+7, daté dans le PV comme engagement post-signature** ; *(à J+7)* preuve écrite envoyée au client — capture `GET …/database` montrant `editionsk896` seule + listing SFTP. Aucune autre action destructrice après signature.
3. **Les emails fonctionnent** : le client envoie et reçoit sur `toutes@editionssociales.fr` en séance ; diff de zones avant/après fourni prouvant que seuls A/CNAME web ont changé.
4. **GEME intact** : `gememarxengels.org` répond, identique.
5. **Archive confirmée** : la confirmation écrite de l'étape 3 est au dossier ; le CSV commandes s'ouvre en séance, la compta y retrouve une commande de son choix. **G7 au dossier** : logs prouvant zéro consommateur de l'API legacy + export compta natif validé par la compta sur une vraie période.
6. **Propriété — effective depuis la mise en production de fin juillet** : le client se connecte à SES comptes — Vercel (projet + facturation sur sa carte, **ressources Neon/Blob visibles sous `ldes` et absentes de `solidz`**), GitHub (repo), Stripe, Brevo, Sentry, Better Stack — et voit Youri en invité, jamais l'inverse (devis §9 mot pour mot).
7. **Le site vit sans Youri** : un push de test sur `main` (fait par le client si possible, guidé) déploie en prod ; la sauvegarde nocturne du jour est présente avec heartbeat vert.
8. **Fonctionnel neuf intact post-clôture** : parcours complet en séance — recherche catalogue, fiche livre, ajout panier, commande test Stripe (remboursée), don test, inscription newsletter, réception des emails transactionnels.
9. **Monitoring** : 14 jours consécutifs verts sur Better Stack au moment de la clôture ; Sentry sans erreur nouvelle non triée ; aucun échec de livraison webhook dans le dashboard Stripe (preuve du nettoyage étape 5.4).
10. **Coûts** : récapitulatif des abonnements au nom du client conforme au devis §8 (~40–47 €/mois courant) ; facture OVH allégée du slot vide ; facturation Vercel/Neon/Blob sur la carte du client **depuis fin juillet**.
11. **Documents** : REVERSIBILITE + RUNBOOK remis, restauration démontrée (étape 10).
12. **PV signé** : critères 1–11 + engagement post-signature du critère 2 + modalités de maintenance (§9) actées. **Références de paiement claires** : le solde de chaque phase était dû à sa recette (le solde commerce à la recette commerce) ; la clôture ne déclenche aucun paiement nouveau sauf reliquat explicitement convenu.

---

## Risques et parades

| Risque | Impact | Parade |
|---|---|---|
| **Transfert Vercel fonctionnellement vert mais incomplet** : le transfert de projet copie les env vars → le site marche, mais Neon/Blob restent possédés et facturés sous `solidz` (la doc exclut le « transfert qui emporte tout ») | Violation silencieuse du §9 (« tout créé au nom de la structure »), facturation chez Youri, découverte tardive | Chemin (b) par défaut (transfert d'intégration + store transfer, étape 9.2) ; **critère de preuve = propriété dans le dashboard `ldes`, pas un test fonctionnel** ; 9-bis mi-juillet ; filet re-création + `pg_dump|pg_restore` + copie Blob (~2 h) ; fallback team-transfer |
| **Transfert exécuté trop tard** (à la clôture au lieu de la mise en production) | Non-conformité au §9, client payant en retard ce qu'il devait payer dès fin juillet | G8 : transfert fin juillet par défaut ; report uniquement sur dérogation écrite du client ; carte + Vercel Pro sur `ldes` fin juillet dans tous les cas |
| **Hotlinks OVH résiduels dans le HTML éditorial** (R3 §6 : ~190 réfs) | Images/PDF cassés silencieusement après extinction | Gate SQL « zéro URL OVH en base » avant l'étape 5 ; garder l'archive uploads → réparation possible a posteriori |
| **Consommateur fantôme de la Legacy REST API / wc-api** (outil compta cassé silencieusement au flip DNS) | Coupure irréversible d'un flux compta au drop de `editionsk884` | **G7** : logs prod sur la fenêtre de recouvrement + identification par la compta + export natif validé sur une vraie période — preuve datée exigée à l'étape 1, avant toute extinction |
| **Commandes/paiements en vol au gel** (Paybox IPN des commandes pending frappe un domaine qui ne sert plus WP) | Commandes payées non enregistrées | La purge (étape 2a) se fait AVANT l'archive ; le gel réel date du flip DNS de la phase commerce — à la Phase 7 il n'y a plus de flux depuis ≥ 14 j ; vérifier `wc-pending` = 0 dans le dump final |
| **Redirection manquante** (URL exotique : shortlinks, `?p=`, flux RSS, ou le host `www.boutique` oublié) | 404 pour des lecteurs/librairies avec vieux liens | `www.boutique` couvert explicitement (G5 + étape 4) ; revue des 404 dans les logs Vercel à J+7 et J+30 après le 301 définitif ; patch de la table (déploiement en minutes) ; le fallback host boutique → `/catalogue` amortit le reste |
| **Résiliation `ladispi` mal séquencée** : `la-dispute.fr` A → 46.105.204.28 meurt ; ou confusion avec le service mail MXPLAN 1000 de la zone (R4 §3.3) | Domaine mort, ou perte de boîtes mail | Gate G6 : repointer le web de `la-dispute.fr` AVANT résiliation ; inventorier les boîtes du MXPLAN ; la résiliation ne vise que `/hosting/web/ladispi…`, jamais `/email/*` |
| **Suppression prématurée** (fichiers ou base avant remise/confirmation, ou pendant la fenêtre) | Perte de données = faute contractuelle | Ordre strict : export → remise → confirmation écrite → **mise hors ligne réversible (rien supprimé)** → +7 j → suppression fichiers + drop DB avec preuve ; G4 verrouille la newsletter |
| **Slot Pro résilié par erreur ou non renouvelé** (expire 2026-12-01) | GEME mort | Interdit écrit dans le PV + rappel de renouvellement dans le RUNBOOK ; aucune commande `terminate` sur ce service dans les scripts |
| **Webhooks Stripe legacy en échec** (endpoints wc-api de l'ancienne gateway sur le compte partagé) | Alertes Stripe en boucle, endpoint désactivé bruyamment, confusion en debug du site neuf | Étape 5.4 : suppression/désactivation des endpoints legacy (test + live) dans le dashboard ; consigné au RUNBOOK ; critère 9 le vérifie |
| **Adaptateur http réintroduit ou env fantômes** | Catalogue vide silencieux (défauts codés en dur → fetch de soi-même) | Étape 7 : suppression complète + grep de non-régression en CI ; tag git `wordpress-era-end` pour l'archéologie |
| **Client indisponible pour la confirmation d'archive** (fermeture d'août en variante non-phasée) | Blocage de toute la chaîne d'extinction | Calage : les confirmations se font hors fermeture (voir calendrier, y compris le re-dump de septembre en B bloc) ; à défaut la fenêtre s'allonge — les WP gelés ne coûtent rien à attendre |
| **Interlocuteur « coquilles » client** (comptes `editionssociales`/`ldes` créés mais identifiants perdus/détenus par ?) | Transfert bloqué | Provisioning vérifié **mi-juillet** (le transfert étant fin juillet) : le client prouve l'accès aux deux comptes et invite Youri membre de `ldes` ; sinon fallback team-transfer ou transfer request initiée côté client |

---

## Dépendances et interfaces avec les autres phases

- **← Phase dons** : Stripe live opérationnel ; la jauge/le checkout doivent survivre au transfert de fin juillet (test de non-régression, étape 9).
- **← Phase prod (hardening / mise en production)** : fournit les 302 (`statusCode: 302`, pas `permanent: false` — voir étape 4) et la table de redirections initiale, l'abaissement des TTL, la résiliation `ladispi` (G6), les hostnames `cms-*`, **et exécute le transfert de propriété de fin juillet selon le protocole de l'étape 9** (G8). La Phase 7 consomme la table (flip 301), vérifie le transfert, puis supprime les `cms-*`.
- **← Phase catalogue** : fournit le swap d'adaptateur (`catalogue.ts:29`), la migration des médias + réécriture des URLs éditoriales (gate SQL de l'étape 7 la prouve), et normalement l'extinction des 2 WP catalogue (ligne devis 0,5 j) — sinon rattrapage étape 6.
- **← Phase commerce** : fournit l'archive initiale (ligne 0,25 j), la **table de matching produit→livre** (entrée directe de la table 301 — la demander explicitement comme livrable de phase, format JSON `{productSlug: {edition, slug}}` incluant les 20 orphelins et leur cible), le flip DNS boutique **apex + www.boutique** + 302, le gel WP, **et les preuves G7** (logs wc-api sur la fenêtre + validation compta de l'export natif).
- **← Phase newsletter** : G4 (Brevo peuplé) avant tout drop de `editionsk884`.
- **← Phase ops (surveillance)** : Better Stack/Sentry/heartbeat en place — la Phase 7 réaffecte les moniteurs et s'appuie dessus pour les critères 7/9.
- **→ Sortie** : phase terminale. Livre l'état final documenté, le PV, et les modalités de maintenance — plus aucune phase en aval.

---

## Calage calendrier

Aujourd'hui : jeudi 2026-07-09. **Jalons communs aux deux variantes (contractuels, indépendants de la date de clôture) :**
- **Mi-juillet (17–20/07, calé)** : étape 9-bis (dry-run transfert d'intégration/store sur projet jetable) + provisioning client vérifié : accès GitHub `editionssociales`, team `ldes` avec carte + Vercel Pro, Youri invité membre de `ldes`.
- **Fin juillet (fenêtre de bascule unique, 24–28/07 proposée, cf. `plan/02-mise-en-production.md`)** : **exécution du transfert de propriété par la phase 2 (E9, en fin de fenêtre)** selon le protocole de l'étape 9 — protocole de référence du projet (repo, projet, intégration Neon, store Blob, comptes stack) — preuves de propriété archivées. À partir de là, la structure paie ses abonnements (§9) et Youri travaille en invité.

**Variante « B phasé » (recommandée et probable — commerce en septembre) :**
- Recette commerce + flip DNS boutique (Phase 6) : hypothèse ~ven 25/09/2026. **Solde de la phase commerce dû à cette recette** (pas à la clôture d'octobre).
- Fenêtre de recouvrement : 14 j → jusqu'au ~ven 09/10, avec collecte des logs G7 pendant la fenêtre.
- **Semaine de clôture : 12–16 octobre 2026.** Lun 12 : étapes 1–2 (audit, gates, dump final, restauration test). Mar 13 : étapes 3–4 (remise + confirmation écrite d'archive ; validation écrite puis flip 301). Mer 14 : étapes 5–7 (mise hors ligne réversible boutique + nettoyage Stripe, rattrapage catalogue, nettoyage code). Jeu 15 : étape 9 volet clôture (vérification du transfert de juillet, rotation, ménage `solidz`). Ven 16 : étapes 10–11 (dossiers + recette de clôture + PV, avec l'engagement post-signature daté).
- **Étape 8 (suppression fichiers + drop des bases) : ~ven 23/10**, après la semaine tampon — **preuve écrite envoyée au client le jour même** (second temps du critère 2).

**Variante « B d'un bloc » (recette complète avant fermeture d'août) :**
- Gel + archive + confirmation : dernière semaine de juillet (~27–31/07), **avant** la fermeture — la confirmation écrite d'archive ne peut pas attendre un client absent. Le transfert de propriété tombe la même semaine (mise en production).
- Fenêtre de recouvrement : tout août (WP gelés, coût nul, Youri surveille — il est là le 15/08 pour le lancement de campagne). Logs G7 collectés sur toute la fenêtre.
- **Au retour du client (~lun 31/08), AVANT toute extinction** : purge des commandes `wc-processing` restantes (étape 2a), **re-dump final de `editionsk884`, remise complémentaire + nouvelle confirmation écrite** — la règle « le dump post-solde fait foi » s'applique aussi ici ; l'archive de juillet ne suffit pas si des commandes ont été soldées depuis.
- Extinctions (phase A) + vérification transfert + clôture : **semaine du 31/08 au 04/09/2026** ; étape 8 (suppressions) ~11/09 avec preuve écrite.

**Effort estimé vs vendu — écart signalé, pas gonflé :**
- Réaliste : étapes 1–8 ≈ 1,0–1,25 j ; étape 9 ≈ 0,5 j exécuté fin juillet (+ 9-bis 0,25 j mi-juillet + 0,25 j de vérification en clôture) ; étapes 10–11 ≈ 0,75 j → **≈ 2,25 j au total**, répartis entre juillet et la semaine de clôture, plus l'attente calendaire (fenêtres).
- Vendu explicitement pour ce périmètre : ~0,25 j (moitié « extinction boutique » de la ligne « Surveillance + extinction » à 0,25 j, + reliquat 301 de « Mise en production »). L'archive (0,25 j) et l'extinction catalogue (0,5 j) sont chiffrées dans leurs phases : si elles ont été faites là-bas, elles ne comptent pas ici. Le transfert de fin juillet émarge naturellement à la ligne « Mise en production » (1 j) dont il est un engagement (§9).
- **Écart : ~+1,5 j non chiffrés en ligne propre** — mais le transfert de propriété, la réversibilité et la recette sont des **engagements fermes du devis §9** (« tout est créé au nom de votre structure », « transférée sans perte d'historique au plus tard à la mise en production », « recette complète avec l'équipe ») : ils font partie du forfait, pas d'un avenant. À traiter en planification, pas en facturation. Si le transfert de ressources part en filet (b)(iv) (re-création Neon/Blob), compter +0,5 j — toujours dans l'épaisseur du forfait, à signaler au client si ça déborde.

---

## Questions ouvertes / décisions client

| # | Question | À trancher au plus tard | Défaut recommandé |
|---|---|---|---|
| Q1 | **Fenêtre de recouvrement boutique** : durée du gel avant extinction | À la recette commerce (Phase 6) | **14 jours** — assez pour un cycle complet commandes+expéditions+compta sur le système neuf, et pour collecter les logs G7 |
| Q2 | **Drop des bases MySQL** vs les laisser dormir (incluses, gratuites) | Étape 8 (J+7 après mise hors ligne) | **Drop après dumps remis + fenêtre tampon** — des données clients dans une base non maintenue sont un passif RGPD, et « fini » veut dire fini ; la base Joomla `1413613-1` suit le même sort |
| Q3 | **Cible GitHub** : compte utilisateur `editionssociales` (coquille existante, plan Free) ou organisation au nom de la structure. ⚠️ Sur un **repo privé en plan Free, la protection de branche/rulesets n'existe pas** (GitHub Pro/Team requis) | Mi-juillet (avant le transfert de fin juillet) | **Compte `editionssociales` existant, sans protection de branche** (documentée au RUNBOOK — un seul dev, CI sur PR) ; options si le client veut la protection : GitHub Pro ~4 $/mois payé par lui, ou org en plan Team ; migrable en org plus tard en 10 min |
| Q4 | **Sort de `dev.editionssociales.fr`** (install dev sur le slot Pro, hors périmètre strict) | Étape 8 | **Archiver + détacher + supprimer les records** — c'est une scorie ; mais décision client car hors périmètre vendu (coût : ~15 min) |
| Q5 | **Google Search Console après les 301 définitifs de la boutique** — les propriétés GSC existent et le sitemap est soumis **depuis juillet** (phase 2, E2.5) ; il ne reste en octobre que le volet boutique post-301 : soumission du sitemap à jour / changement d'adresse pour `boutique.editionssociales.fr` | Clôture (optionnel) | **~30 min sur les propriétés existantes, avec le client** (propriété GSC au nom du client, ou transfert tracé — P9 phase 2) ; les 301 font l'essentiel du travail SEO seuls ; ne pas l'ajouter au forfait |
| Q6 | **Maintenance post-clôture** : rester sur le régime §9 (corrective gratuite, nouveau besoin chiffré, aucun abonnement) ou souscrire une veille mensuelle (hors devis) | À la recette de clôture, dans le PV | **Régime §9 tel quel** — c'est l'engagement vendu ; une veille payante ne se propose que si le client la demande |
| Q7 | **Conservation de la team `solidz`** après transfert : le projet et les ressources partis, la team redevient un espace Youri | Clôture (étape 9, volet vérification) | Aucune action requise côté client ; Youri fait le ménage de ses tokens (rotation) et vérifie qu'aucune ressource facturable ne reste sous `solidz` |
| Q8 | **Boîtes mail actives sur le MXPLAN 1000 de `la-dispute.fr`** (surprise R4) : à inventorier avec le client — conditionne uniquement l'attention lors de G6, jamais une résiliation dans cette phase | Avant G6 (phase prod), re-vérifié étape 1 | **Ne rien toucher** ; consigner l'inventaire dans le RUNBOOK |
| Q9 | **Contrat Paybox** : après le cutover commerce, la passerelle Paybox (le vrai PSP historique — R2 §2.4) ne sert plus à rien mais son contrat continue peut-être de facturer | Après la fenêtre de recouvrement | **Recommander la résiliation par le client** (démarche commerciale côté Paybox, hors périmètre technique) ; consigner au RUNBOOK ; ne jamais la faire à sa place |
| Q10 | **Report du transfert de propriété** (si le client préfère que tout reste chez Youri jusqu'à la clôture) | Mi-juillet | **Non recommandé** (contraire au §9) ; possible uniquement par **dérogation écrite** datée, et dans tous les cas carte + Vercel Pro sur `ldes` dès fin juillet |

---

*Fin du plan Phase 7. Résumé exécutable : transférer la propriété à la mise en production (fin juillet — repo, projet Vercel, intégration Neon + store Blob séparément, preuves de propriété, pas de test fonctionnel en guise de preuve) → geler → archiver → faire confirmer → rendre les redirections définitives (statusCode 301, sur accord écrit) → mettre hors ligne réversiblement (DNS + détachement, fichiers et bases intacts 7 jours) → nettoyer le code (adaptateur http retiré, port conservé, tag `wordpress-era-end`) et les webhooks Stripe legacy → remettre réversibilité + runbook → PV de clôture sur les 12 critères, avec un unique engagement post-signature : le drop fichiers+bases à J+7, prouvé par écrit. Aucune étape ne détruit quoi que ce soit avant remise vérifiée ni pendant la fenêtre tampon ; le slot Pro (GEME) et les MX ne sont jamais touchés.*