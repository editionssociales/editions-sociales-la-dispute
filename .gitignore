# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# claude code local worktrees/settings
.claude/

# typescript
*.tsbuildinfo
next-env.d.ts

# payload — médias uploadés en local (dev sans Vercel Blob)
/media

# payload — rapports + fichiers temporaires de la migration WordPress→Postgres
scripts/migrate-catalogue/out/
scripts/import-orders-woo-out/

# inventaire de vérification des redirections (`build-redirect-inventory.mjs`) :
# régénéré à chaque étape (E5, E6, E7), jamais commité — à la différence de
# `src/lib/redirects-produits.json`, qui LUI est versionné (décision d'arbitrage
# du plan, cf. next.config.ts).
scripts/redirect-inventory.csv
